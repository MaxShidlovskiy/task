const a = +(prompt("Введите число A"));
const b = +(prompt("Введите число B"));
const c = +(prompt("Введите число С"));
const d = +(prompt("Введите число D"));
if (a < b,
    a < c,
    a < d) {
    console.log(a);
} else if (b < a,
           b < c,
           b < d) {
    console.log(b);
} else if (c < a,
           c < b,
           c < d) {
    console.log(b);
} else if (d < a,
           d < b,
           d < c) {
    console.log(b);
} 